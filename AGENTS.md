# database_dev_skills 维护指南

本仓库用于沉淀可复用的数据库研发 skills 及其提示词模板。内容应保持轻量、可审查，并能直接复制到 Codex 工作流中使用。

## 目录约定

- 每个 skill 位于仓库根目录下以其 skill 名称命名的目录中；保留 `SKILL.md`、`agents/`、`assets/`、`references/` 和 `scripts/` 等 skill 自身需要的结构。
- 提示词模板统一位于 `prompt_templates/<模板或对应-skill-名称>/prompt_template.md`。
- `prompt_template.md` 是模板目录中面向使用者的唯一入口；目录名应稳定、语义清楚，并使用小写 ASCII 名称、连字符或下划线。
- 不要将生成的 Wiki、下载的源码、构建产物、测试缓存或密钥提交到此仓库。

## Skill 维护原则

- `SKILL.md` 的 frontmatter 中必须保留准确的 `name` 和 `description`，并使目录名与 `name` 保持一致。
- 将可复用的页面骨架放入 `assets/`，将需要在执行前阅读的规则放入 `references/`，将可执行的检查工具放入 `scripts/`；不要把大量实例项目内容混入 skill。
- 修改 skill 时保持其工作流、证据等级和验证声明一致。尤其是数据库正确性、事务、持久化、复制、恢复与故障路径，不能以外部文章替代当前源码或测试证据。
- 优先进行小而完整的改动。除非任务明确要求，否则不重排无关文件、不批量改写已有文档，也不改变已发布 skill 的路径。

## 提示词模板原则

- 模板应可直接使用，明确输入变量、预期产物、范围边界和验证要求。
- 保留用户提供的事实性约束、链接和占位符；仅为目录归档而移动模板时，不改写其内容。
- 外部资料只能作为理解或检索线索。涉及实现、版本、提交、行为或正确性的结论，模板必须要求回到官方资料、当前源码或可复现验证。
- 对检索和研究类模板，明确官方来源优先级、纳入与排除标准、缺失数据处理，以及输出格式和列语义。

## 验证与提交

- 提交前检查 `git diff --check`，并确认暂存文件全部属于当前任务。
- 新增或修改脚本时，至少运行其可用的语法或帮助检查；不要把静态检查结果表述为构建、运行时或正确性验证。
- 一个提交只承载一个清晰目的。skill、模板和仓库治理文档的无关改动应拆分提交。
- 提交说明遵循 Conventional Commits：`<type>(optional scope): <description>`。`type` 使用小写英文；`scope` 可选，应为受影响的目录或组件名；`description` 使用简洁的祈使式英文，准确概括实际变更。
- 常用 `type`：新增可用 skill 或模板用 `feat`；修复仓库内容错误用 `fix`；仅文档、模板或参考资料变更用 `docs`；测试变更用 `test`；不改变外部行为的重构用 `refactor`；维护性或工具性变更用 `chore`。只有破坏性变更才使用 `!` 或 `BREAKING CHANGE:`。
- 推送前确认目标分支和远端；重写已推送历史前必须明确确认，并只使用 `git push --force-with-lease`。
